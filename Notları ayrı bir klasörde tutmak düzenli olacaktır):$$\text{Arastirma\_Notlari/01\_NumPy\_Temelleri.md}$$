# 🔍 Araştırma Notu: NumPy Temelleri

## NumPy Nedir?
* Python'da bilimsel hesaplama ve büyük veri dizileri (array/matris) ile çalışmak için kullanılan temel kütüphanedir.
* Python'daki standart listelere göre çok daha hızlıdır ve daha az bellek kullanır.

## Kurulum ve Dahil Etme
* **Kurulum Komutu:** `pip install numpy`
* **Kullanım Komutu:** `import numpy as np` (genellikle `np` takma adı kullanılır)

## Temel Yapı: ndarray
* NumPy'ın ana nesnesi **N-boyutlu dizi** anlamına gelen `ndarray`'dir.
* **Oluşturma Örneği:**
    ```python
    import numpy as np
    # Tek boyutlu dizi (Vektör)
    dizi = np.array([1, 2, 3, 4, 5]) 
    # İki boyutlu dizi (Matris)
    matris = np.array([[1, 2], [3, 4]])
    ```

## Tamamlanan Görev
* Proje Takibi: NumPy kütüphanesi temel eğitim araştırması
$$\text{docs: NumPy kütüphanesi temel araştırma notları eklendi}$$
